# Generated by scripts/generate-devcontainer.ts — edit shared/docker/Dockerfile.rust and re-run.

FROM rust:1-bookworm

WORKDIR /workspace

RUN apt-get update -qq && apt-get install --no-install-recommends -y curl openssh-client tree
