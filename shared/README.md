# shared/

**言語をまたぐ**共通設定のソースです。`yarn generate:configs` で各テンプレートにコピーされます。

- **prettier/** — `.prettierrc.json`（全テンプレート）
- **editorconfig/** — `.editorconfig`（全テンプレート）

編集後は `yarn generate:configs` を実行して反映してください。
