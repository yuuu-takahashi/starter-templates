# Sinatra (dotenv). Copy to .env and set values. Do not commit .env.
APP_ENV=development
RACK_ENV=development

# Database (config/database.yml 等で参照)
DATABASE_HOST=db
DATABASE_USER=root
DATABASE_PASSWORD=password
DATABASE_NAME=template-sinatra_development
