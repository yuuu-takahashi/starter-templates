# Rails (dotenv-rails). Copy to .env and set values. Do not commit .env.
RAILS_ENV=development

# Database (config/database.yml)
DATABASE_HOST=localhost
DATABASE_USER=root
DATABASE_PASSWORD=
# Optional: DATABASE_NAME overrides per-environment DB name

# Redis (config/cable.yml) — optional
# REDIS_URL=redis://localhost:6379/1
