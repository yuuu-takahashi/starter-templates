{
  "name": "template",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:ci": "node scripts/generate-root-workflow.js",
    "generate:devcontainer": "node scripts/generate-devcontainer.js",
    "generate:configs": "node scripts/generate-configs.js",
    "generate:all": "yarn generate:ci && yarn generate:devcontainer && yarn generate:configs"
  },
  "devDependencies": {
    "yaml": "^2.6.1"
  }
}
