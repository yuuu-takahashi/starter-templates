# Generated by yarn generate:ci - edit shared/workflows/ or template's .github/workflows/ instead.

name: Devcontainer Build

on:
  pull_request:

defaults:
  run:
    shell: bash

concurrency:
  group: '${{ github.workflow }}-${{ github.head_ref || github.ref_name }}'
  cancel-in-progress: true
jobs:
  changes:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    outputs:
      nextjs: ${{ steps.filter.outputs.nextjs }}
      nodejs: ${{ steps.filter.outputs.nodejs }}
      react: ${{ steps.filter.outputs.react }}
      rails: ${{ steps.filter.outputs.rails }}
      rails_api: ${{ steps.filter.outputs.rails_api }}
      sinatra: ${{ steps.filter.outputs.sinatra }}
      ruby: ${{ steps.filter.outputs.ruby }}
      full_nextjs: ${{ steps.filter.outputs.full_nextjs }}
      full_rails_api: ${{ steps.filter.outputs.full_rails_api }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |-
            nextjs:
              - 'minimal-templates/nextjs/.devcontainer/**'
            nodejs:
              - 'minimal-templates/nodejs/.devcontainer/**'
            react:
              - 'minimal-templates/react/.devcontainer/**'
            rails:
              - 'minimal-templates/rails/.devcontainer/**'
            rails_api:
              - 'minimal-templates/rails-api/.devcontainer/**'
            sinatra:
              - 'minimal-templates/sinatra/.devcontainer/**'
            ruby:
              - 'minimal-templates/ruby/.devcontainer/**'
            full_nextjs:
              - 'full-templates/nextjs/.devcontainer/**'
            full_rails_api:
              - 'full-templates/rails-api/.devcontainer/**'
  nextjs:
    needs: changes
    if: needs.changes.outputs.nextjs == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: minimal-templates/nextjs/.devcontainer/Dockerfile
          failure-threshold: error
  nodejs:
    needs: changes
    if: needs.changes.outputs.nodejs == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: minimal-templates/nodejs/.devcontainer/Dockerfile
          failure-threshold: error
  react:
    needs: changes
    if: needs.changes.outputs.react == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: minimal-templates/react/.devcontainer/Dockerfile
          failure-threshold: error
  rails:
    needs: changes
    if: needs.changes.outputs.rails == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: minimal-templates/rails/.devcontainer/Dockerfile
          failure-threshold: error
  rails_api:
    needs: changes
    if: needs.changes.outputs.rails_api == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: minimal-templates/rails-api/.devcontainer/Dockerfile
          failure-threshold: error
  sinatra:
    needs: changes
    if: needs.changes.outputs.sinatra == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: minimal-templates/sinatra/.devcontainer/Dockerfile
          failure-threshold: error
  ruby:
    needs: changes
    if: needs.changes.outputs.ruby == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: minimal-templates/ruby/.devcontainer/Dockerfile
          failure-threshold: error
  full_nextjs:
    needs: changes
    if: needs.changes.outputs.full_nextjs == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: full-templates/nextjs/.devcontainer/Dockerfile
          failure-threshold: error
  full_rails_api:
    needs: changes
    if: needs.changes.outputs.full_rails_api == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: full-templates/rails-api/.devcontainer/Dockerfile
          failure-threshold: error
